[build]
  base = "DAMS.Web"
  command = "npm ci && npm run build"
  publish = "dist/DAMS.Web/browser"

# SPA fallback (also covered by public/_redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
